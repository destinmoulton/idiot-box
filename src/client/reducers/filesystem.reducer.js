import {

} from '../actions/actionTypes';

const INITIAL_STATE = {

};

export default function filesystemReducer(state = INITIAL_STATE, action){
    switch(action.type){

        default:
            return state;
    }
}